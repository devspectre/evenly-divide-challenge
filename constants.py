# TODO: this should be part of Django settings(settings.py)
# in actual Django project and it should read these from environment variables
# Need validation code to check if these constants are valid
MINUTE_INTERVAL = 15
HOUR_INTERVAL = 3
